(function($){

    $.su = $.su || {};   $.su.CHAR = $.su.CHAR||{};
 $.su.CHAR.SETTING ={
     NAPT: {
         CHECK_COLUMN_WIDTH: 32,
         ROW_NUMBER_WIDTH: 40,
         NAME_WIDTH: 150,
         IP_RANGE_WIDTH: 120,
         INTERFACE_WIDTH: 60,
         STATUS_WIDTH: 50,
         COMMENT_WIDTH: 80,
         SETTING_WIDTH: 100
     },
       "" :     ""
    };
})(jQuery);